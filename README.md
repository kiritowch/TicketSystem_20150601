注意事项：
1、此课程设计为车站售票管理系统，分为网站和.exe程序两部分，分别为用户和售票员使用；
2、数据库备份文件名为 “数据库备份文件”，原数据库环境为SQL Sever 2000，直接还原数据库即可；
3、点击train.exe即可运行售票员程序（一账号为admin，密码password），点击 /用户版（PHP网站）/login.html即可登录（一账号为admin1，密码123）；
4、原运行环境为windows xp，SQL Sever 2000,服务器为Apache，语言PHP5.x,另外需配置SQL Sever的ODBC数据源，默认账号为sa，密码369568，名称为train1。